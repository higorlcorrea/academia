@model GrupoMuscularModel
@{
    ViewBag.Title = "Grupo Muscular";
    ViewBag.Acao = "EDITAR";
}

<div id="page-wrapper" class="page-wrapper-cls">
    <div id="page-inner">
        <div class="row">
            <div class="col-md-12">
                <h1 class="page-head-line">Grupo Muscular</h1>
            </div>
        </div>        
        @{Html.BeginForm("Editar", "GrupoMuscular", FormMethod.Post);}
        @Html.Partial("_Form", Model)
        @{Html.EndForm();}
    </div>
</div>
@section Js{
    @Scripts.Render("~/jqueryval")
}
