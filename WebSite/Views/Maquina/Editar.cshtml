@model MaquinaModel
@{
    ViewBag.Title = "Máquina";
    ViewBag.Acao = "EDITAR";
}

<div id="page-wrapper" class="page-wrapper-cls">
    <div id="page-inner">
        <div class="row">
            <div class="col-md-12">
                <h1 class="page-head-line">Máquina</h1>
            </div>
        </div>
        @{Html.BeginForm("Editar", "Maquina", FormMethod.Post);}
        @Html.Partial("_Form", Model)
        @{Html.EndForm();}
    </div>
</div>
@section Js{
    @Scripts.Render("~/jqueryval")
}
